spring:
  # H2 TCP 모드 설정 (로컬 개발용)
  datasource:
    url: jdbc:mysql://62.171.156.211:3306/vacationdb?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: 1000bang
    password: dlfdl12#01
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  h2:
    console:
      enabled: false
  
  jpa:
    hibernate:
      ddl-auto: create-drop  # 로컬 환경: 테이블 재생성 (division, team 컬럼 제거)
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

# JWT 설정 (로컬 환경)
jwt:
  secret: vacationapplication_jwtsecret_madeby1000bang  # 로컬 환경 기본값
  expiration: 3600000  # Access Token: 1시간 (밀리초)
  refresh-expiration: 604800000  # Refresh Token: 7일 (밀리초)

# 로그 설정은 logback-spring.xml에서 관리
# logging 설정을 여기에 두면 logback-spring.xml의 설정을 오버라이드할 수 있음

# 파일 업로드 설정 (로컬 환경)
file:
  upload:
    path:
      local: C:/Downloads/upload
  # 서명 파일 저장 경로 설정
  signature:
    path:
      local: C:/Downloads/signature
