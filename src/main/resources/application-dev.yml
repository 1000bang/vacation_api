spring:
  # H2 TCP 모드 설정 (개발용)
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    hibernate:
      ddl-auto: update  # 개발 환경에서는 update 사용
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

# JWT 설정 (개발 환경)
jwt:
  secret: vacationapplication_jwtsecret_madeby1000bang  # 개발 환경 기본값
  expiration: 3600000  # Access Token: 1시간 (밀리초)
  refresh-expiration: 604800000  # Refresh Token: 7일 (밀리초)

logging:
  level:
    com.vacation.api: DEBUG
    org.springframework.web: INFO

# 파일 업로드 설정 (개발 환경)
file:
  upload:
    path:
      dev: /Users/1000bang/Downloads/upload
      local: C:/Downloads/upload
